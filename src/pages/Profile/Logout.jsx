import React, { useContext } from 'react'

import { Navigate, useNavigate } from 'react-router-dom'
const Logout = () => {
  // const [on, setOn] = useContext(ModalContext)
  const navigate = useNavigate()
  return (<h1>Logging Out...</h1>)
   
  
}

export default Logout